services:
  - type: pserv
    name: auth_service
    env: docker
    dockerfilePath: ./authServer/Dockerfile
    autoDeploy: true

  - type: pserv
    name: profile_service
    env: docker
    dockerfilePath: ./profileServer/Dockerfile
    autoDeploy: true

  - type: pserv
    name: blog_service
    env: docker
    dockerfilePath: ./blogServer/Dockerfile
    autoDeploy: true

  - type: web
    name: api_gateway
    env: docker
    dockerfilePath: ./api-gateway/Dockerfile
    autoDeploy: true